google-api-python-client==2.147.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
yt-dlp[default]==2026.7.4
# Потрібен yt-dlp для роботи з socks5:// проксі (YT_DLP_PROXY_URL).
PySocks==1.7.1
# Потрібен для --impersonate: без нього yt-dlp падає з
# 'Impersonate target "chrome" is not available'. Екстра [default] його НЕ
# тягне — перевірено на реальному прогоні #842. Імперсонація потрібна, бо
# частину форматів YouTube віддає лише клієнтам, схожим на справжній
# браузер, інакше 403 на завантаженні медіа.
curl_cffi==0.16.3
requests==2.32.3
# НЕ піднімати без перевірки: на openai>=2.0.0 виклик Whisper
# (audio.transcriptions з whisper-1) починає віддавати 404 і весь дубляж
# падає. gpt-image-2 тут не заважає — thumbnail_generator ходить в
# /v1/images/edits напряму через requests, без SDK.
openai==1.51.0
httpx==0.27.2
Pillow==10.4.0
python-dotenv==1.0.1

# Опційно: потрібен лише якщо перейдете на Cloudflare R2 замість тунеля
# (див. scripts/r2_storage.py і розділ про альтернативи в README)
# boto3==1.35.36
